const log = require('./../logging/log');
const _ = require('lodash');

// TODO : Implement BlockChain Interfacing
class BlockChainAdapter {

    constructor() {
    }

    put(task) {
        return {"tracking_number" : 12, "reward_per_task" : 12};
    }

    execute() {

    }
}

module.exports = BlockChainAdapter;